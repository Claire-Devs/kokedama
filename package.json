{
  "name": "kokedama",
  "version": "0.1.0",
  "private": true,
  "description": "A static digital garden generator for Markdown notes.",
  "scripts": {
    "build": "node build.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
